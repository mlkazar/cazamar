Ideas for Radio app

1 record multiple streams

2 record based on schedule (like alarm setting code)

3 back and skip buttons

4 save recordings.  some UI to select true start and end of song.

5 use photopea for drawing

========Todo========

** change default to always record

** use keyword searches to do AND not OR

** highlight  should search back in history  for right station

** station prefix should be on lock screen, not regular scroll

** radiotime, at least, sometimes sends back \n separated list of URLs as a string.
   So, trim whitespace from URLs, and maybe check until you find a working one from
   the list.

** Help text for search won't be read if long, and is way too long.

** Sample station in search results.

-- limit results to 64 stations chosen randomly (and warn user).  Do
   this in radioscan, before starting validation.  Do it by generating
   a probability of 64/count, and flipping a random coin over one pass
   picking up or discarding a station based on the coin flip.  Perhaps limit to
   128 now that validation is faster.

-- keep using disk file across app restarts

-- settings for how much music to keep / whether to restore after
   restart / whether to automatically record playing stations in background

-- break out 'search station' as a separate button on hte menu line.

** reorganize UI so press always selects, long pres brings up status
   with a button for editing, which does a second push.

** see if can improve ad text to get more views

** Search should be populating live without popup.

** multiple validation threads

** prefix station 6 char abbrev to song names

** history should eliminate recent (10) duplicates

** try not to play silence when music is playing

** garbage collect free space from large 

** replace all dispatch_async calls with performSelectorOnMainThread
   calls instead.

** if we're not the current station and there's a network failure,
   restartRadio tries to shutdown the currently playing station, not
   the one that had the failure.  Leaves the stream's pthread exited,
   but the stream isn't shutdown.  Should cleanly shutdown the stream,
   and make a callback to something that just restarts the station's
   stream with the station's existing buffer.

   Add a weak pointer from the AqStream to the SignStation.  On
   callback, if it isn't the playing station, just restart the
   station's stream (new function).

** check out warnings and obsolete interfaces used.

** popup after changing highlights or background streaming

** slider for scrobbing in popup, showing time and current song

** reanimate song bar after switching back to main screen

** finish help text for search and main help files

** provide visual indication of object being dragged

** generic help and context sensitive (for search) help

** double check that songs are labelled properly esp. when seeking
   around

** improve recording status visibility by flashing?

** reorder by drag and drop

** basic record button.  When recording, don't shutdown stream.
   Indicate visually.

** icon-done.png is really ugly: green is glaring.  Also text label
   color needs to be black

** highlight button on popup status screen

** search is ugly when searching / stop button needs to be clearer

** Stall a bit until buffers are loaded when starting new station (was bug)

** edit existing radio station

** Clicking on blank area should display useful stuff like hihghlight
and web stream info (partly done)

** Add stop button.

** bullet proof playing code

** make sure multi-word queries are properly escaped ('\020' or '+') in
HTTP queries in RadioScan.

** +20 / -20 buttons

** Automatically figure out if browing or searching for a single
station (pretty much always browse, but watching try treating as a
name as well)

** clearer done and cancel buttons during search

** some way to see which station is playing.

** trim saved music stream after N minutes

** incremental image population for search bar and main screen

** save history per station (or globally).  The latter is probably better.

** current song needs to be represented in file data stream.

** search is a little confusing when done

** fix shutdown bug in stream player ignoring pthreadDone

** sync song title change with return of buffer (reduced buffer size)

** History should successfully scroll to bottom.  Need to add
"showHistory" method that does the pushView + scroll operation.

** some visual queue that station search is complete

** Session management so that song titles appear on lock screen.

** hiccups during Bach play

** stop doesn't stop very fast

** redo UI for displaying station details (speed, type, URLs)

** RPC timeout takes too long when we switch to/from 5G.

** doesn't reconnect after a failiure.


================Longer term items================

-- modify MFANSocket to support https?

-- keep playing silent noise when downloading and nothing else playing

-- alarm clock functionality?

-- Some way when saving a recording to edit the start and end times.

-- smart record functionality -- by song boundary

-- export recordings somehow

** use backing file for large streams

-- shazam button to identify playing song.

================================================================

Stations:
WYEP
WMFO http://webstream.wmfo.org/;type=http
WERS
Acid Flashback
WSUM
KKUP
Jaxx Radio Black Music
North East Radio Scotlandf
WESU
WESA
KQED
J.S.Bach Classical Radio
