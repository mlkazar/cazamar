* fixup names starting with invalid characters

* verify that getPath is required for getattr.  It is required
for lookup, not getattr.

* move account for app registration to michael.kazar@verizon.net

================

cfssnap

================

Modify SApi so that a new page only has to define a method, not a whole class.

